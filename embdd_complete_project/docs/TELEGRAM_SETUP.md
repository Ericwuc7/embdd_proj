# Telegram Setup

1. Create a bot using BotFather and copy bot token.
2. Get your chat ID.
3. Set in `raspberry_pi/.env`:

```env
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_CHAT_ID=your_chat_id
```

4. Test by running full system script.
